# 模型名称正则匹配说明

正则匹配是一种灵活的模型名称匹配方式，用于将渠道中的模型自动分组到对应的组。以下是具体示例：

---

## 匹配类型对比

| 类型 | 说明 | 组名称 | 匹配的模型 |
|-----|------|-------|-----------|
| 模糊匹配 (Fuzzy) | 模型名包含组名 | `gpt-4` | `gpt-4o`, `gpt-4-turbo`, `gpt-4.5` |
| 精确匹配 (Exact) | 模型名完全等于组名 | `gpt-4o` | `gpt-4o` (仅此一个) |
| 正则匹配 (Regex) | 使用正则表达式匹配 | `^gpt-4` | 所有以 `gpt-4` 开头的模型 |

---

## 正则匹配示例详解

### 示例 1：匹配 GPT-4 系列所有模型

```
正则表达式: ^gpt-4
```

| 模型名称 | 是否匹配 |
|---------|---------|
| `gpt-4` | ✅ |
| `gpt-4o` | ✅ |
| `gpt-4-turbo` | ✅ |
| `gpt-4.5` | ✅ |
| `gpt-3.5-turbo` | ❌ |

---

### 示例 2：匹配特定结尾的模型

```
正则表达式: -turbo$
```

| 模型名称 | 是否匹配 |
|---------|---------|
| `gpt-4-turbo` | ✅ |
| `gpt-3.5-turbo` | ✅ |
| `gpt-4o` | ❌ |

---

### 示例 3：同时匹配多个模式

```
正则表达式: (gpt-4|claude-3)
```

| 模型名称 | 是否匹配 |
|---------|---------|
| `gpt-4o` | ✅ |
| `claude-3-opus` | ✅ |
| `claude-3-sonnet` | ✅ |
| `gpt-3.5-turbo` | ❌ |

---

### 示例 4：使用通配符 `.` 和 `*`

```
正则表达式: gpt-4\..+
```

| 模型名称 | 是否匹配 |
|---------|---------|
| `gpt-4.5` | ✅ |
| `gpt-4.o1` | ✅ |
| `gpt-4o` | ❌ (点后面没有字符) |

---

### 示例 5：忽略大小写匹配

```
正则表达式: (?i)GPT-4
```

| 模型名称 | 是否匹配 |
|---------|---------|
| `gpt-4` | ✅ |
| `GPT-4` | ✅ |
| `Gpt-4` | ✅ |

---

## 常用正则表达式语法速查表

| 语法 | 含义 | 示例 |
|-----|------|------|
| `^` | 开头匹配 | `^gpt-` 匹配以 `gpt-` 开头的模型 |
| `$` | 结尾匹配 | `-turbo$` 匹配以 `-turbo` 结尾的模型 |
| `.` | 任意单个字符 | `gpt-4.` 匹配 `gpt-4o`, `gpt-4a` |
| `*` | 0个或多个 | `gpt-4.*` 匹配 `gpt-4`, `gpt-4o`, `gpt-4-turbo` |
| `+` | 1个或多个 | `gpt-4.+` 匹配 `gpt-4o`, `gpt-4-turbo` (不匹配 `gpt-4`) |
| `|` | 或 | `gpt-4\|gpt-3.5` 匹配 `gpt-4` 或 `gpt-3.5` |
| `( )` | 分组 | `(gpt-4\|claude-3)` 分组后使用 `|` |
| `\d` | 数字 | `gpt-4-\d` 匹配 `gpt-4-1`, `gpt-4-2` |
| `\w` | 字母数字下划线 | `gpt-\w` 匹配 `gpt-4`, `gpt-a` |
| `[]` | 字符集 | `[46]-turbo` 匹配 `4-turbo` 或 `6-turbo` |
| `?i` | 忽略大小写 | `(?i)gpt-4` 匹配 `gpt-4`, `GPT-4`, `Gpt-4` |

---

## 实际应用场景

### 场景 1：OpenAI 所有模型

```
正则表达式: ^(gpt-4|gpt-3\.5|o1|o3)
```

| 模型名称 | 是否匹配 |
|---------|---------|
| `gpt-4o` | ✅ |
| `gpt-3.5-turbo` | ✅ |
| `o1-preview` | ✅ |
| `o3-mini` | ✅ |
| `claude-3-opus` | ❌ |

---

### 场景 2：Claude 3 系列

```
正则表达式: claude-3.*
```

| 模型名称 | 是否匹配 |
|---------|---------|
| `claude-3-opus` | ✅ |
| `claude-3-sonnet` | ✅ |
| `claude-3-haiku` | ✅ |
| `claude-2` | ❌ |

---

### 场景 3：Google Gemini 系列

```
正则表达式: gemini-1\.(5|2)
```

| 模型名称 | 是否匹配 |
|---------|---------|
| `gemini-1.5-pro` | ✅ |
| `gemini-1.5-flash` | ✅ |
| `gemini-2.0-flash` | ✅ |
| `gemini-1.0-pro` | ❌ |

---

### 场景 4：模型编号精确匹配

```
正则表达式: gpt-4o-(2024-05-13|2024-07-18)
```

| 模型名称 | 是否匹配 |
|---------|---------|
| `gpt-4o-2024-05-13` | ✅ |
| `gpt-4o-2024-07-18` | ✅ |
| `gpt-4o-2024-11-01` | ❌ |

---

## 调试技巧

### 在线正则测试工具
- [regex101.com](https://regex101.com) - 支持多种正则风格，实时匹配
- [regexr.com](https://regexr.com) - 简洁的正则测试工具

### 常见错误
1. **忘记转义特殊字符**：`.` 需要写成 `\.`
2. **贪婪匹配**：使用 `.*?` 来非贪婪匹配
3. **字符集中的特殊含义**：`-` 在 `[]` 内部可能表示范围，需要放在开头或结尾

---

## 总结

正则匹配特别适合以下场景：

1. **复杂的模型命名规则** - 如同时支持多个前缀/后缀
2. **跨版本模型** - 如同时匹配 `gpt-4`, `gpt-4.0`
3. **忽略大小写** - 渠道返回的模型名大小写不一致时
4. **多提供商聚合** - 一个组包含多个提供商的模型

> **提示**：Octopus 使用 ECMAScript 正则语法，如需高级功能请参考 ECMAScript 规范。
